# monitoring namespace — VictoriaMetrics stack (VMSingle, VMAgent, Grafana, Alertmanager).
# prometheus-node-exporter needs host network/pid access → privileged.
apiVersion: v1
kind: Namespace
metadata:
  name: monitoring
  labels:
    pod-security.kubernetes.io/enforce: privileged
    pod-security.kubernetes.io/warn: privileged
