# PostgreSQL credentials for CloudNativePG bootstrap.
# ============================================================
# This file is SOPS-encrypted. The cleartext structure is:
#
#   apiVersion: v1
#   kind: Secret
#   metadata:
#     name: fluodata-pg-credentials
#     namespace: fluodata
#   type: kubernetes.io/basic-auth
#   stringData:
#     username: fluodata
#     password: <your-password-here>
#
# To create this secret:
#   1. Generate an age keypair (see docs/sops-workflow.md)
#   2. Create the cleartext YAML above in a temp file
#   3. Encrypt: sops --encrypt --age <PUBLIC_KEY> secret.yaml > secret.enc.yaml
#   4. Replace this file with the encrypted version
#
# IMPORTANT: Never commit the cleartext version to git.
# ============================================================
apiVersion: v1
kind: Secret
metadata:
  name: fluodata-pg-credentials
  namespace: fluodata
type: kubernetes.io/basic-auth
stringData:
  username: fluodata
  password: CHANGE_ME_BEFORE_ENCRYPTING
