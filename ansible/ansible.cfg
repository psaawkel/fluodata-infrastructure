[defaults]
# No default inventory — config comes from env folder:
#   ansible-playbook deploy.yml -e env=../proxmox-homelab
#   ansible-playbook destroy.yml -e env=../ovh-prod
roles_path = roles
stdout_callback = default
