[defaults]
# No static inventory — hosts are added dynamically via add_host in playbooks.
# Usage:
#   ansible-playbook proxmox-deploy.yml  -e env=../environments/proxmox-homelab
#   ansible-playbook proxmox-destroy.yml -e env=../environments/proxmox-homelab
#   ansible-playbook vps-deploy.yml      -e env=../environments/vps-prod
#   ansible-playbook vps-destroy.yml     -e env=../environments/vps-prod
roles_path = roles
stdout_callback = default
