# Terraform (legacy, archived)
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.*.backup
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
**/terraform.tfvars

# Ansible
*.retry

# Sensitive cluster files (may appear in various locations)
**/kubeconfig
**/talosconfig
**/secrets.yaml

# Environment folders — gitignored (contain secrets + generated files).
# Only *-example/ folders are committed.
environments/proxmox-*/
environments/vps-*/
!environments/proxmox-example/
!environments/vps-example/

# OS
.DS_Store
Thumbs.db
