# Terraform (legacy, archived)
**/.terraform/
*.tfstate
*.tfstate.backup
*.tfstate.*.backup
*.tfplan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
**/terraform.tfvars

# Ansible
*.retry

# Sensitive cluster files (may appear in various locations)
**/kubeconfig
**/talosconfig
**/secrets.yaml

# Environment folders — generated/runtime files are gitignored.
# config.yml and node-facts-*.yml are SOPS-encrypted and safe to commit.
environments/*/controlplane.yaml
environments/*/worker.yaml
environments/*/patch-*.yaml
environments/*/*.txt
environments/*/kubelet-serving-cert-approver.yaml
environments/*/argocd-root-app.yaml

# Example environments (always committed, never encrypted)
# Real environments: config.yml is SOPS-encrypted, committed to git.

# Age private key — NEVER commit this
*.agekey
keys.txt

# OS
.DS_Store
Thumbs.db
